package com.butingbe.domain.notification.dto.response;

/** Response DTO representing zone push notification subscription status. */
public record ZoneSubscriptionResDto(String zoneId, boolean isActive) {}
